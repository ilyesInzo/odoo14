<?xml version="1.0" encoding="UTF-8"?>
<odoo noupdate="1">

        <!-- Update existing rule : Employee can not modify validated timesheets -->
        <record id="hr_timesheet.timesheet_line_rule_user" model="ir.rule">
            <field name="name">account.analytic.line.timesheet.user</field>
            <field name="model_id" ref="analytic.model_account_analytic_line"/>
            <field name="perm_write" eval="0"/>
            <field name="perm_unlink" eval="0"/>
        </record>

        <record id="timesheet_line_rule_user_update-unlink" model="ir.rule">
            <field name="name">account.analytic.line.timesheet.user.update-unlink</field>
            <field name="model_id" ref="analytic.model_account_analytic_line"/>
            <field name="domain_force">[
                ('user_id', '=', user.id),
                ('validated', '=', False),
                ('project_id', '!=', False),
                '|',
                    ('project_id.privacy_visibility', '!=', 'followers'),
                    '|',
                        '|',
                            ('project_id.allowed_internal_user_ids', 'in', user.ids),
                            ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                        ('task_id.message_partner_ids', 'in', [user.partner_id.id])
            ]</field>
            <field name="groups" eval="[(4, ref('hr_timesheet.group_hr_timesheet_user'))]"/>
            <field name="perm_create" eval="0"/>
            <field name="perm_write" eval="1"/>
            <field name="perm_unlink" eval="1"/>
            <field name="perm_read" eval="0"/>
        </record>

    <record model="ir.rule" id="call_duty_user_rule_display">
        <field name="name">Call-on duty : user: see only his call-on duty</field>
        <field name="model_id" ref="model_call_duty"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4,ref('hr_timesheet.group_hr_timesheet_user'))]"/>
    </record>

    <record model="ir.rule" id="call_duty_approver_rule_display">
        <field name="name">Call-on duty : Approver see all call-on duty</field>
        <field name="model_id" ref="model_call_duty"/>
        <field name="domain_force">['|',('employee_id.parent_id.user_id', '=', user.id),('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4,ref('hr_timesheet.group_hr_timesheet_approver'))]"/>
    </record>

    <record model="ir.rule" id="call_duty_manager_rule_display">
        <field name="name">Call-on duty: managers ee all call-on duty</field>
        <field name="model_id" ref="model_call_duty"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('hr_timesheet.group_timesheet_manager'))]"/>
    </record>

</odoo>
